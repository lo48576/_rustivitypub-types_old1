language: rust
cache: cargo
rust:
  # Uncomment to enable `stable` and `beta` when `TryFrom` become stable.
  #- stable
  #- beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
notifications:
  email: false
